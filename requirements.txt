# CityPulse backend — Phases 1-9
# Install with: pip install -r requirements.txt
Flask>=3.1
flask-cors>=5.0
python-dotenv>=1.0
psycopg[binary]>=3.2        # PostgreSQL driver (Supabase-compatible)
requests>=2.32              # provider HTTP (Open-Meteo, OpenAQ, GTFS-RT)
gtfs-realtime-bindings>=1.0 # GTFS-Realtime protobuf decoding (optional runtime)
gunicorn>=23.0              # production WSGI server (Render/Railway/free tiers)
pytest>=8.3                 # backend smoke tests
